{{ namespace }}
{{ key }}
{% unless product.metafields['{{ namespace | downcase }}']['{{ key | downcase }}'] == empty %}
    <a href="{{ product.metafields['{{ namespace | downcase }}']['{{ key | downcase }}'] }}" download>
        <span>{{ link_text }}</span>
    </a>
{% endunless %}